/**
 * Legacy Overview tab (mission-control modal path).
 * Body was corrupted; stub keeps the module importable until the tab is rebuilt or removed.
 */
export function OverviewTab() {
    return null;
}
